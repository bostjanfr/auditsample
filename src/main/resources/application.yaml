spring:
  datasource:
    url: jdbc:h2:file:E:/company/projects/auditsample/DB/mytestDB.mv.db
    password:
    username: sa
    driver-class-name: "org.h2.Driver"
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: false


  h2:
    console:
      enabled: true
      path: /h2